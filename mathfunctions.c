#include <stdio.h>
#include <math.h>

int main(){

    double A = sqrt(9); //squre root
    double B = pow(2,4); // power
    int C = round(3.14); // round off
    int D = ceil(3.14); // round highest
    int E = floor(3.99); //round lowest
    double F = fabs(-100);// absolute
    double G = log(3); //logarthm
    double H = sin(45);
    double I = cos(45);
    double J = tan(45);

}